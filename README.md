
energenie
=========

Remotely control power sockets from the Raspberry Pi.

This is a simple wrapper around the _energenie_ python package (https://pypi.python.org/pypi/energenie) providing `switchOn`, `switchOff`, `switchOnSync` and `switchOffSync` functions.
